// src/pages/ProductsPage.jsx
import React from 'react';
import Products from '../components/Products'; // adjust the path if needed

const ProductsPage = () => {
    return (
        <div>
            <Products/>
        </div>
    );
};

export default ProductsPage;
